// TODO: Cliente REST para backend
// - Base URL configurable
// - JWT auth (token en localStorage)
// - Métodos: login, createTicket, validateTicket, getResults, getReports
export const API_BASE = import.meta.env.VITE_API_URL || 'http://localhost:4097/api';
